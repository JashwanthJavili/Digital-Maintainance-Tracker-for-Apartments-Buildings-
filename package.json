{
  "name": "digital-maintenance-tracker",
  "version": "1.0.0",
  "description": "Digital Maintenance Tracker for Apartments and Buildings",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run admin\" \"npm run resident\" \"npm run technician\"",
    "backend": "cd backend && npm run dev",
    "admin": "cd frontend/admin-frontend && npm start",
    "resident": "cd frontend/resident-frontend && npm start",
    "technician": "cd frontend/technician-frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend/admin-frontend && npm install && cd ../resident-frontend && npm install && cd ../technician-frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
