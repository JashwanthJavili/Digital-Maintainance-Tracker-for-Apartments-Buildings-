/**
 * User roles enum
 */
export enum Roles {
  Resident = 'Resident',
  Technician = 'Technician',
  Admin = 'Admin',
}

/**
 * Issue status enum
 */
export enum Status {
  New = 'New',
  Assigned = 'Assigned',
  InProgress = 'In-Progress',
  Resolved = 'Resolved',
}
